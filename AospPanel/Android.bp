android_app {

    name: "AospPanel",

    static_libs: ["SystemUI-libs"],

    resource_dirs: ["res"],

    certificate: "platform",
    optimize: {
        enabled: false,
        proguard_flags_files: ["proguard.flags"],
    },
    platform_apis: true,

    srcs: ["src/**/*.java"],

}
