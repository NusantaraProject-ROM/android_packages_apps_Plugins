android_app {

    name: "AospPanel",

    libs: ["SystemUI-libs"],
    static_libs: ["ResMini"],

    resource_dirs: ["res"],

    certificate: "platform",
    optimize: {
        enabled: false,
        proguard_flags_files: ["proguard.flags"],
    },
    platform_apis: true,

    srcs: ["src/**/*.java"],

}
