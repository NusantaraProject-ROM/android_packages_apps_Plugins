android_app {

    name: "MiuiPanel",

    static_libs: [
        "VolumePluginCommon",
        "org.lineageos.platform.internal",
    ],

    resource_dirs: ["res"],

    certificate: "platform",
    optimize: {
        enabled: false,
        proguard_flags_files: ["proguard.flags"],
    },
    platform_apis: true,

    srcs: ["src/**/*.java"],

}
